# CI / local development toolchain.
# app/logic and app/data are Streamlit-free by design, so tests only need these.
pandas~=2.2
pytest~=8.3
ruff~=0.16
# AppTest page-smoke suite (tests/test_pages_apptest.py)
# Floors kept in lockstep with requirements.txt: streamlit~=1.52 (width='stretch'
# API) forbids altair 5.4.0/5.4.1, so altair~=5.5 is the lowest co-installable.
streamlit~=1.52
altair~=5.5
jinja2>=3.1.2
mypy~=1.14
sqlglot>=30.12.0  # migration SQL parse gate (tests/test_migrations_parse.py)

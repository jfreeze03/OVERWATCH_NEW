"""OVERWATCH — Snowflake usage command center (rebuild).

Intentionally empty: importing ``app`` must never trigger Streamlit or
Snowflake imports, so the logic/data layers stay testable in bare CI.
"""

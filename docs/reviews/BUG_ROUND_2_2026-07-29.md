# Bug round 2 — 2026-07-29 (post-v4.70.1)

7-domain sweep over surfaces the metrics triage did not cover (UI/state wiring, logic engines,
alert pipeline, ops/security/admin, unaudited loader procs, deep cost pages, mart-vs-live parity),
every candidate adversarially verified (refute-by-default).

**41 findings survived** · counts: {"confirmed": 40, "plausible": 1, "app": 32, "mig": 9, "high": 8, "medium": 15, "low": 18, "critical": 0, "merged_duplicates": 4}

_Synthesis notes:_ 41 net-new defects survived triage (40 CONFIRMED, 1 PLAUSIBLE; 45 survivor entries deduped by merging 4 pairs: tuning direction-blindness, resize-savings booking, Tasks-tab schema filter, lock-wait windows) — no criticals, but three incident-response write paths (emergency levers, cancel-query, incident declare) are fully dead behind the r27 allow-list, daily false HIGH anomalies pollute the flagship triage queue, and the documented 90-day rebuild path silently loads 2 days; dominant themes are allow-list vs UI drift, mart/live parity gaps (windows, filters, anchors), and loader/backfill coverage holes — 32 fixes are app-side Python, 9 need migrations.

### B1. [HIGH · CONFIRMED · APP] Emergency pipe/task/user/account levers are always blocked by the executor allow-list
- **Surface:** Operations → Emergency tab (operator levers) · `app/ui/pages/operations.py:850`
- All ALTER PIPE/TASK/USER/ACCOUNT levers route through execute_statement, whose _WRITE_PREFIXES (query.py:271-278) admits only ALTER WAREHOUSE + OVERWATCH DML/CALL, so every non-warehouse lever is refused while the audit INSERT still logs a FAILED row.
- **Fix:** Add the exact builder-generated lever prefixes ('ALTER PIPE ', 'ALTER TASK ', 'ALTER USER ', 'ALTER ACCOUNT SET ') to _WRITE_PREFIXES and extend the test_r27_app lock, or pre-check _statement_allowed and render blocked levers copy-only with an explanatory caption.

### B2. [HIGH · CONFIRMED · APP] Running-queries kill-switch (SYSTEM$CANCEL_QUERY) can never execute
- **Surface:** Operations → Emergency tab (cancel running query) · `app/ui/pages/operations.py:903`
- The cancel runs 'SELECT SYSTEM$CANCEL_QUERY(...)' through execute_statement, which rejects any non-write-prefix statement, so the advertised kill switch is structurally dead and every attempt logs STATUS='FAILED' to REMEDIATION_LOG.
- **Fix:** Add a dedicated execute_cancel_query(qid) seam in query.py that regex-validates the query id and builds/executes the exact SELECT itself (or add a full-match regex allow-list entry for this one shape); keep the audit INSERT.

### B3. [HIGH · CONFIRMED · APP] Incident declare from proposals can never succeed: SET statement blocked by operator allow-list
- **Surface:** Control Room → Declare incident + link alerts · `app/ui/pages/control_room.py:349`
- _incident_declare_sql opens with 'SET OW_INC_ID = UUID_STRING();' which _statement_allowed rejects, so the session variable never exists and both INSERTs fail server-side — the manual declare button writes zero INCIDENTS/INCIDENT_MEMBERS rows, ever.
- **Fix:** Generate the incident id app-side (uuid.uuid4()) and inline it via sql_literal into both INSERTs, dropping the SET statement entirely.

### B4. [HIGH · CONFIRMED · APP] Anomaly surfaces score today's partial FACT_WAREHOUSE_DAILY row, generating false spend anomalies every morning
- **Surface:** Control Room triage queue + Cost/Spend + Operations anomaly panels · `app/ui/pages/control_room.py:377`
- SP_LOAD_HOURLY_FACTS writes a growing partial row for today and all three app callers feed it to flag_anomalies unfiltered, so a steady warehouse's part-day spend scores z<=-5 and stamps recurring false SEVERITY=HIGH triage rows daily — while the server twin SP_ANOMALY_SWEEP explicitly bounds DAY < CURRENT_DATE().
- **Fix:** In all three callers (control_room.py:377, cost_parts/spend.py:287, operations.py:526) drop today's row before scoring, e.g. df[pd.to_datetime(df['DAY']).dt.date < account_today()], mirroring SP_ANOMALY_SWEEP; keep mart_sql.fact_warehouse_daily unchanged for trend charts.

### B5. [HIGH · CONFIRMED · MIG] backfill_365 'HOURLY', 90 silently loads only 2 days for the four LEAST(:d,2)-capped mart arms
- **Surface:** Documented rebuild/recovery backfill path (SP_LOAD_MARTS_V27 qfam/tagcov/alloc/alloc_xdim) · `snowflake/backfill_365.sql:137`
- The V056 LEAST(:d,2) cap on the four extract-fed day-grain arms survives into the latest SP_LOAD_MARTS_V27, so the advertised 90-day family/allocation backfill loads 2 days while RETURNing 'loaded (HOURLY, 90d)' — silent, and unrecoverable once ACCOUNT_USAGE ages past 365d.
- **Fix:** Re-derive SP_LOAD_MARTS_V27 so the four capped arms clamp to actual extract coverage (lower bound GREATEST(-:d, MIN(START_TIME) of OW_QH_EXTRACT)) or add an explicit backfill-mode parameter lifting the cap; until then correct backfill_365.sql:103-105/137 and FULL_REBUILD.md step 5.

### B6. [HIGH · CONFIRMED · APP] Jump-to box is dead: consume_pending_navigation wipes the pick before it is read
- **Surface:** Sidebar Jump-to navigation (all users) · `app/core/state.py:91`
- The pre-widget clear of _ow_jump runs before the early return on every rerun, and a session-state write to a not-yet-instantiated widget key overrides the frontend-delivered selection, so every Jump-to pick is erased on the rerun that delivers it — the entire surface is a silent no-op (empirically reproduced on Streamlit 1.58).
- **Fix:** In consume_pending_navigation(), pop _ow_nav_pending first, return if empty, and only THEN clear st.session_state['_ow_jump'] so the box resets only when a queued navigation is actually consumed.

### B7. [HIGH · CONFIRMED · APP] Database picks from live inventory are silently reverted by init_filters' static-list validation
- **Surface:** Sidebar database filter, saved views, deep links · `app/core/state.py:37`
- The picker offers live SHOW DATABASES classified by pattern rules, but init_filters wipes any flt_database not in the static ALFA/TREXIS tuples — so DBA_MAINT_DB and any new ALFA_*/TRXS_* database is un-picked on the next run, and applied saved views are silently unscoped in the same run they are applied.
- **Fix:** Validate with the same classification rules the picker uses (classify_database/classify_environment match against the selected company/env, e.g. db.upper() not in classify_databases((db,), company, env)) instead of static-tuple membership; keep databases_for() as offline fallback only.

### B8. [HIGH · CONFIRMED · APP] Cross-page navigation writes _ow_nav_radio without validating against the viewer's profile pages
- **Surface:** Alerts drawer 'Investigate →', saved DEFAULT_VIEW landing, profile-gated nav · `app/core/state.py:99`
- consume_pending_navigation writes _ow_nav_radio unconditionally, so an EXECUTIVE-profile viewer clicking Investigate on a PERF_/SEC_ alert either crashes the radio once then lands on the off-profile page (Streamlit 1.45, requirements floor) or gets a silently dead button (1.58); empirically reproduced on both versions.
- **Fix:** Clamp in consume_pending_navigation: resolve the viewer's profile pages via resolve_role_profile/PAGES_BY_PROFILE and only write the radio when the page is offered, else fall back to 'Overview'; belt-and-braces drop stale _ow_nav_radio values in _sidebar before st.radio (the components.py:101-106 pattern).

### B9. [MEDIUM · CONFIRMED · MIG] SP_NOTIFY_WEBHOOK marks events delivered that were truncated out of the 3000-char message
- **Surface:** Hourly webhook delivery + ALERT_DELIVERIES ledger + delivery SLO panel · `snowflake/migrations/V034__route_company_filter.sql:109`
- The sender caps the LISTAGG message at LEFT(:message, 3000) but the ALERT_DELIVERIES insert re-evaluates the full eligibility predicate with no cap awareness, so backlogged oldest events (including CRITICALs) are permanently marked delivered, never re-sent, and counted green by delivery_slo_summary.
- **Fix:** Re-derive the proc to ledger only what fit: compute the eligible set with a running SUM(LENGTH(line)) over the same RAISED_AT DESC order and insert delivery rows only for lines within the 3000-char cap, leaving the rest eligible for the next hourly run (or send in fixed-size batches).

### B10. [MEDIUM · CONFIRMED · MIG] Nightly reconcile rebuilds hour-grain surfaces 3d deep from a 72h extract — boundary hour permanently undercounted
- **Surface:** MART_OPS_DIAG_HOURLY + role/schema hourly marts (06:00 D-3 bucket, daily) · `snowflake/migrations/V056__loader_reconcile_alert_fixes.sql:912`
- SP_NIGHTLY_RECONCILE resets the extract to exactly now-3d (~06:46) then rebuilds hour-grain marts 3d deep, so the previously-complete 06:00 D-3 bucket is rebuilt/MERGE-overwritten from ~14 of its 60 minutes with no repair path — systematically hiding failures in the exact hour the platform's own 06:40/06:45 daily task tree runs.
- **Fix:** Re-derive SP_NIGHTLY_RECONCILE to keep rebuild depth inside extract coverage: pass 2 (not 3) to SP_LOAD_OPS_DIAG and align role/schema-hourly deletes/merges accordingly, or start the hour-grain rebuild at the first FULL hour inside the extract (DATE_TRUNC('hour', MIN(START_TIME)) + 1 hour).

### B11. [MEDIUM · CONFIRMED · MIG] FACT_WAREHOUSE_DAILY and FACT_QUERY_DAILY have no catch-up path — a multi-day task outage leaves permanent interior holes
- **Surface:** Core spend/query facts feeding exec board and daily-spend charts · `snowflake/migrations/V041__loader_efficiency.sql:328`
- Both facts load from fixed -3d/-2d windows with no watermark (unlike SP_LOAD_DAILY_FACTS, which got exactly this treatment), and backfill_365's guards only fill days older than MIN(DAY), so a >3-day task outage — a documented, historically-observed class — leaves an unfillable interior hole and under-reported credits.
- **Fix:** Re-derive the loaders: give the FACT_WAREHOUSE_DAILY merge the SP_LOAD_DAILY_FACTS watermark-minus-1d catch-up (clamped -30d), make the FACT_QUERY_DAILY arm honor DAYS_BACK in backfill mode, and/or convert backfill_365's only-older-than-MIN guards to per-day anti-joins.

### B12. [MEDIUM · CONFIRMED · MIG] backfill_365 races the hourly extract task — a mid-backfill trim can wipe up to 87 days of ops-diag history
- **Surface:** Backfill/rebuild runs vs TASK_LOAD_HOURLY (minute-7 hourly) · `snowflake/backfill_365.sql:136`
- The script never suspends the task graph, so the hourly extract's watermark DELETE can trim the freshly-filled 90-day extract mid-run; if it lands before SP_LOAD_OPS_DIAG(90)'s delete-then-rebuild, up to ~87 days of accumulated FAIL_FAMILY/TOP_ELAPSED history is destroyed silently on a re-run of the advertised-idempotent script.
- **Fix:** Bracket the backfill with ALTER TASK TASK_LOAD_HOURLY SUSPEND before line 136 and RESUME + SYSTEM$TASK_DEPENDENTS_ENABLE after line 144; defense-in-depth, re-derive SP_LOAD_OPS_DIAG to clamp its DELETE window to GREATEST(requested bound, extract MIN(START_TIME)).

### B13. [MEDIUM · CONFIRMED · APP] Contract pacing 'Consumed' silently under-counts when neither fact nor live source reaches the contract start
- **Surface:** Cost → Contract pacing KPIs, steering plan, renewal planner · `app/ui/pages/cost_parts/contract.py:274`
- The mart path is coverage-gated (FACT_FIRST_DAY <= contract start) but the live fallback's FIRST_DAY is never checked, so for contracts older than ~12 months the retention-truncated METERING sum is presented as the full term — consumed share, pace ratio, projections and renewal math all read wrong-low under a caption claiming 'since contract start'.
- **Fix:** After the live path serves, compare res.df FIRST_DAY to the contract start; when FIRST_DAY > start, annotate the panel as a lower bound and mark pace/projection KPIs partial (or suppress pace math), mirroring the mart_accept standard, and propagate the flag to steering-plan and renewal numbers.

### B14. [MEDIUM · CONFIRMED · APP] Live-fallback advisors divide 90-day-clamped scan data by the unclamped 180/365 window
- **Surface:** Cost → Optimize idle advisor, sizing recommendations, what-if, ledger bookings · `app/ui/pages/cost_parts/optimize.py:123`
- The live builders clamp at 90d while the page passes raw 180/365 filter days into every monthly-izer (idle_advisor, size_recommendations, simulate_scenario, monthly_savings_estimate), understating monthly dollars and booked ESTIMATED amounts ~2-4x and flipping genuine 'Size up' warehouses to 'Keep' via the QUEUED_MIN_PER_DAY gate.
- **Fix:** Compute eff_days = days if res.source is the mart source else min(days, 90) (run_mart_first carries the source) and pass eff_days to all four logic-layer call sites plus the flagged-warehouse ledger INSERT amounts; longer-term expose the effective window from the builder layer.

### B15. [MEDIUM · CONFIRMED · APP] Resize savings booking ignores the chosen target size and direction — a resize UP still books the half-rate 'down' saving
- **Surface:** Cost → Optimize resize execute + REMEDIATION_LOG / SAVINGS_LEDGER · `app/ui/pages/cost_parts/optimize.py:219`
- est_sz is gated only on RECOMMENDATION.startswith('SIZE DOWN') and computed as a blind 0.5x scenario — the 'Resize to' selectbox target is never reconciled, so a resize UP or same-size no-op books a positive ESTIMATED saving (DESCRIPTION even says 'Resize X to LARGE'), a multi-step down books only 1 step, and XSMALL warehouses can be flagged down-candidates with no smaller size existing.
- **Fix:** Fetch the warehouse's current size (SHOW WAREHOUSES, already used by _whatif_panel) and derive est_sz from the step delta: est_sz = round(MONTHLY_USD_NOW * (1 - 2**applied_delta), 2) only when applied_delta < 0, book 0 with a cost-increase warning otherwise, and suppress RECOMMEND_DOWN for known-XSMALL warehouses in size_recommendations.

### B16. [MEDIUM · CONFIRMED · APP] Company-scoped DDL/DCL feed AND-joins user- and database-grain clauses, creating rows no company lens can ever show
- **Surface:** Security → recent DDL/DCL changes ('Grants' stack) · `app/data/security_sql.py:187`
- recent_ddl_changes ANDs user_clause with a NULL-intolerant database allow-list, so account-level GRANT/REVOKE/ALTER_SESSION rows with NULL DATABASE_NAME are excluded under ALFA and Trexis lenses and (via the user clause) under UNKNOWN too — visible only under company=ALL while DEFAULT_COMPANY is ALFA, contradicting the codebase's own 'NULL stays visible everywhere' doctrine.
- **Fix:** Replace database_clause(company) with database_visibility_clause(company) (written for exactly this semantic), or OR a 'DATABASE_NAME IS NULL' arm into the company database predicate so user-classified no-context DDL/DCL stays visible under that user's company lens.

### B17. [MEDIUM · CONFIRMED · APP] Threshold tuning is direction-blind — suggestions inverted for lower-bound rules (SEC_CRED_EXPIRY, COST_CONTRACT_BREACH)
- **Surface:** Alerts → threshold tuning suggestions panel · `app/logic/tuning.py:45`
- suggest_threshold universally assumes higher METRIC_VALUE = worse, but SEC_CRED_EXPIRY (days-to-expiry) and COST_CONTRACT_BREACH (days-left) fire when the value is LOW — both the pure-noise branch (line 45, escalating to current*1.5) and the midpoint branch (line 59) propose RAISING the threshold, which widens the firing window while the basis string claims it cuts noise; the METRIC_VALUE>0 filter (line 32) also silently discards already-expired events.
- **Fix:** Add a per-rule direction map (LOWER_BOUND_RULES = {'SEC_CRED_EXPIRY', 'COST_CONTRACT_BREACH'}) passed into suggestions_by_rule; for those rules either invert the math (suggest below the noise floor, keep actioned by <= comparisons, drop the >0 filter) or — simpler and safe — exclude them with a basis explaining day-window rules need manual tuning.

### B18. [MEDIUM · CONFIRMED · APP] compare_release_periods gives verdict 'n/a' for from-zero regressions, letting the panel declare 'No query-health regression'
- **Surface:** Operations → Release compare (query-health verdict banner) · `app/logic/insights.py:130`
- A zero baseline maps delta_pct to None and any None to 'n/a', so a release that introduced a 12% failure rate, queueing, and spill from a clean baseline renders a green 'No query-health regression' banner — while sibling wh_change.change_deltas explicitly treats from-zero as 'worse' ('something from nothing').
- **Fix:** For lower_better metrics: b == 0 and a > 0 → 'Worse' (delta_pct None), b == 0 and a == 0 → 'Flat'; keep 'n/a' only for lower_better is None; add a zero-baseline regression test.

### B19. [MEDIUM · CONFIRMED · APP] task_release_deltas omits RUNS_BEFORE/RUNS_AFTER from the pivot backfill, crashing the Release-compare panel when all task rows fall on one side
- **Surface:** Operations → Release compare (task deltas table) · `app/logic/insights.py:165`
- With one-sided input (e.g. release date today, AFTER side empty under ACCOUNT_USAGE lag) the pivot emits only observed PERIOD columns and the backfill loop fills FAILED_*/AVG_SEC_* but not RUNS_*, so the display selection raises KeyError "['RUNS_AFTER'] not in index" and safe_page replaces the Operations page render.
- **Fix:** Extend the backfill tuple to ('RUNS_BEFORE', 'RUNS_AFTER', 'FAILED_BEFORE', 'FAILED_AFTER', 'AVG_SEC_BEFORE', 'AVG_SEC_AFTER'); add a one-sided-input regression test.

### B20. [MEDIUM · CONFIRMED · APP] Operations Tasks tab silently ignores the schema filter whenever the mart serves
- **Surface:** Operations → Tasks tab (KPIs + table vs schema-filtered RCA timeline) · `app/ui/pages/operations.py:458`
- _tasks_tab reads mart_sql.fact_task_daily first (no schema param or column) and only the rare live fallback honors schema_contains, so with the filter set the KPIs/table are unfiltered while the failure-RCA timeline directly below IS filtered — contradictory numbers on one screen; the sibling _queries_tab comment proves the intended gate.
- **Fix:** Mirror the _queries_tab contract: when schema_contains is non-empty skip the mart and call ops_sql.task_runs directly, OR add a schema_contains param to mart_sql.fact_task_daily (FACT_TASK_DAILY carries SCHEMA_NAME and day_task_failures already reads it).

### B21. [MEDIUM · CONFIRMED · APP] Prepared big-table CSV blob is never invalidated — 'CSV ready' downloads stale rows after filters change
- **Surface:** All big-table CSV exports (two-step prepare/download) · `app/ui/components.py:651`
- The prep click stores df.to_csv() bytes under a positional key that nothing ever clears; after a Company/window change the same table position reuses the key and '⬇ CSV ready' serves the old scope's rows verbatim with no staleness label while the on-screen table shows the new scope.
- **Fix:** Store a fingerprint (len(df), columns, current filters/refresh salt) beside the blob and drop back to the prepare state on mismatch; additionally sweep all ow_dlprep_* keys in page_header alongside the _ow_dl_seq reset (also fixes the known memory-retention concern).

### B22. [MEDIUM · CONFIRMED · APP] Table download-widget identity breaks inside fragments: _ow_dl_seq only resets in page_header
- **Surface:** Big-table CSV export inside fragments (Alerts open-events, Operations emergency, Optimize what-if) · `app/ui/components.py:635`
- The seq counter resets only in page_header (outside every fragment), so any fragment rerun re-registers the download button under a new key and the click's trigger resolves to a dead widget — big-table CSV prepare is a silent no-op on every attempt inside a fragment.
- **Fix:** Stop deriving widget identity from a run-order counter: use the caller's stable key when present (seq only for the default filename) and for unkeyed tables derive the suffix from stable content identity (hash of columns + page + section) or a fragment-scoped counter reset at fragment top.

### B23. [MEDIUM · CONFIRMED · APP] Default-landing/timezone retry is defeated by the app's own ?page= write
- **Surface:** Session-open saved DEFAULT_VIEW landing + DISPLAY_TZ application · `app/main.py:217`
- Both retry paths return without setting _ow_default_applied, but run 1's sidebar writes ?page= into the URL, so run 2 mistakes the self-written param for a user deep link and marks applied — one transient probe/prefs failure on the first run silently drops the saved landing AND display timezone for the whole session, making the deliberately-built 3-attempt retry unreachable.
- **Fix:** Capture initial deep-link presence once before the sidebar ever runs (st.session_state['_ow_initial_deeplink'] = bool(st.query_params.get('page')) on first run) and test that flag at line 217, or mark remember_page's self-writes and ignore them in _apply_default_landing.

### B24. [LOW · CONFIRMED · MIG] Undelivered-expired watchdog ignores family/severity route scope — permanent hourly error-log noise for below-threshold events
- **Surface:** Hourly watchdog → APP_ERROR_LOG ('undelivered_expired') · `snowflake/migrations/V034__route_company_filter.sql:153`
- The out-of-delivery-scope carve-out checks COMPANY_FILTER only while route eligibility also gates on FAMILY and MIN_SEVERITY, so a MEDIUM event under the seeded ALL/HIGH route is counted expired every hourly run (~24 rows/day) — the exact spam class the carve-out's own comment says it prevents, plus a false 'check ALERT_ROUTES' signal.
- **Fix:** Re-derive the watchdog EXISTS to mirror full route eligibility: join ALERT_CONFIG for the event's family and add the severity-rank comparison, counting an event only if some enabled route could actually carry it.

### B25. [LOW · CONFIRMED · APP] Routing panel hides COMPANY_FILTER — ALFA-only routes render as if they deliver both companies
- **Surface:** Alerts → routes panel (delivery diagnosis) · `app/data/mart_sql.py:460`
- alert_routes() omits COMPANY_FILTER — the one column that silently excludes a company's events from delivery since the V034 ALFA backfill — so an operator sees an ENABLED matching route and concludes routing is correct while company scoping is the real cause; the panel help and example INSERT never mention it either.
- **Fix:** Add COALESCE(COMPANY_FILTER,'ALL') AS COMPANY_FILTER to the alert_routes() SELECT, extend the example INSERT at alerts.py:659-665, and mention the ALFA-only V034 default in the panel_help text.

### B26. [LOW · CONFIRMED · APP] Alert-fatigue REPEAT_EVENTS is structurally always zero — dead 'dedupe repeats' signal
- **Surface:** Alerts → fatigue panel · `app/data/mart_sql.py:1070`
- Every ALERT_EVENTS writer guards inserts with an unbounded NOT EXISTS on DEDUPE_KEY, so two rows can never share a key and COUNT(*) - COUNT(DISTINCT key) is a constant 0 — the advertised 'dedupe repeats' attention-cost column measures nothing while storm re-raises get new day/week-scoped keys.
- **Fix:** Either drop the column or strip the trailing date/week component from DEDUPE_KEY (REGEXP_REPLACE on the date suffix) and count rows minus distinct stripped keys, surfacing the same condition re-firing across days.

### B27. [LOW · CONFIRMED · APP] Tag-coverage 'Tagged share' KPI is computed from a top-30-most-untagged sample, not the scoped total
- **Surface:** Cost → tag coverage panel (aggregate KPI + warn threshold) · `app/ui/pages/cost.py:130`
- Both readers behind the panel are top-30-untagged-user leaderboards with a 60s floor, so summing them makes the untagged numerator nearly complete while the total-exec denominator drops fully-tagged users — the share is systematically biased low (e.g. 25% shown vs ~78% true), erring toward false alarm at the 30% warn threshold.
- **Fix:** Add an unsampled totals variant of each reader (single row of SUM(EXEC_SEC), SUM(UNTAGGED_EXEC_SEC) over the scope, mart-first from MART_TAG_COVERAGE_DAILY) and use it for the KPI and threshold, keeping the top-30 query for the leaderboard table only.

### B28. [LOW · CONFIRMED · APP] Live-only scan panels label 90-day-clamped results with the selected 180/365 window
- **Surface:** Cost → Optimize expensive patterns / storage growth; Unit costs proc trend · `app/ui/pages/cost_parts/optimize.py:310`
- expensive_patterns_usd, storage_growth_by_database, and proc_cost_trend all clamp at 90d while the page labels totals and captions with the raw 180/365 selection (optimize.py:310/325/423/437-438, unit_costs.py:182) — window labels overstate coverage 2-4x though the numbers themselves are internally consistent.
- **Fix:** Label with the effective window (eff = min(days, 90), same for days_storage) at all five sites, or state the 90d live-scan cap explicitly; cleanest long-term, export the clamp from the builder module so labels cannot drift from the SQL.

### B29. [LOW · CONFIRMED · APP] Failed-logins panel and its 'reasons' decomposition read different windows (30d cap vs 90d cap)
- **Surface:** Security → failed logins + reasons decomposition + export manifest · `app/data/security_sql.py:210`
- failed_logins clamps at 30d while failed_login_reasons clamps at the default 90d, so for 60/90-day windows the reasons totals cover 2-3x the span of the panel they explicitly decompose, with no disclosure when rows render and an overstated export-manifest window line.
- **Fix:** Use bounded_days(days, maximum=30) in failed_login_reasons to match the panel (or caption the reasons panel with its own window and add 'failed logins capped at 30d' to the manifest line).

### B30. [LOW · CONFIRMED · APP] Control Room triage queue drops the schema filter on the task-facts leg
- **Surface:** Control Room → triage queue (task-failure input) · `app/ui/pages/control_room.py:366`
- Same defect class as the Operations Tasks tab: fact_task_daily has no schema arg so schema_contains applies only on the rare live fallback — out-of-scope task failures appear in the 2-day queue with the filter set, and the caption's filter disclosure never mentions the schema drop.
- **Fix:** Share the Operations fix (fall back to ops_sql.task_runs when schema_contains is set, or add a schema_contains param to mart_sql.fact_task_daily) and extend the line-398 caption to state schema-filter behavior for task facts.

### B31. [LOW · CONFIRMED · APP] Lock-wait twins cover different windows: mart up to 14 days, live fallback silently clamped to 7
- **Surface:** Operations → Contention tab (lock waits) · `app/ui/pages/operations.py:581`
- _contention_tab passes min(days, 14) to both legs but ops_sql.lock_contention internally clamps to 7d, so on the live-fallback path (pre-V035 / mart error) the panel aggregates exactly 7 days while the scope and mart-served twin claim up to 14 — silent, since neither source label states a window.
- **Fix:** Pass min(days, 7) to both legs (updating the test_v035_lock_mart pinned strings), or keep the mart at 14d and append the covered window to each source label (e.g. '(7d cap)' on the live label) so the span change is visible.

### B32. [LOW · CONFIRMED · APP] Ops-diag mart pair anchors CURRENT_TIMESTAMP while its live twins and same-screen KPIs anchor CURRENT_DATE
- **Surface:** Operations → ops-diag top-queries/failures tiles vs KPI row · `app/data/mart27_sql.py:780`
- ops_diag_top_queries and ops_diag_failures window on CURRENT_TIMESTAMP while their live twins and the KPI row above use CURRENT_DATE, so the same tile's span shifts up to ~24h depending on which source serves — the exact anchor-mismatch class the 4.68.0/4.70.0 fixes standardized elsewhere but missed here.
- **Fix:** Change both readers' predicates to d.HOUR_TS >= DATEADD('day', -{days}, CURRENT_DATE()) (and the coverage-gate comparison), matching ops_sql._query_scope, and add the pair to the anchor lock test.

### B33. [LOW · CONFIRMED · MIG] SP_PURGE_FACTS (latest, V055) never purges FACT_OBJECT_COST_DAILY, FACT_STORAGE_ACCOUNT_DAILY, or MART_TASK_NODE_DAILY
- **Surface:** Monthly purge task / retention settings · `snowflake/migrations/V055__cloud_services_breakdown.sql:322`
- The latest purge proc covers 25 tables but misses these three (MART_TASK_NODE_DAILY was created in V058, after the last purge re-derivation), and their loaders never trim history, so they grow unboundedly and FACT_RETENTION_DAYS_DAILY is a silent no-op for them — FACT_OBJECT_COST_DAILY plausibly the highest-cardinality fact in the schema.
- **Fix:** Re-derive SP_PURGE_FACTS adding three daily-retention DELETEs (DAY < DATEADD('day', -:daily_days, CURRENT_DATE())) and adopt a checklist rule that any migration creating a fact/mart re-derives the purge proc in the same file.

### B34. [LOW · PLAUSIBLE · MIG] Unguarded DELETE-then-INSERT in SP_LOAD_DAILY_FACTS / SP_LOAD_OBJECT_COST leaves holes or partial arms when a later statement fails
- **Surface:** Daily fact loaders (FACT_TASK/LOGIN/STORAGE_DAILY, FACT_OBJECT_COST_DAILY) · `snowflake/migrations/V045__task_monitoring_restored.sql:93`
- Three DELETE+INSERT pairs (and SP_LOAD_OBJECT_COST's one-DELETE-seven-INSERTs) run with autocommit and no transaction/handler, so a transient mid-proc failure leaves the trailing window missing or cross-arm-inconsistent for 24h and skips the day's chained reconcile/score/mart tasks — the exact hazard r22 #7 fixed in the extract but nowhere else.
- **Fix:** Re-derive both procs wrapping each DELETE+INSERT unit in BEGIN TRANSACTION / COMMIT with an EXCEPTION handler that ROLLBACKs and logs to APP_ERROR_LOG, matching the V056 extract pattern.

### B35. [LOW · CONFIRMED · APP] Density and timezone controls live in a fragment, so applying them repaints nothing
- **Surface:** Views popover (Compact density toggle, Save timezone) · `app/main.py:141`
- _views_popover is @st.fragment, but the compact CSS is emitted by inject_theme() in the main body and all timestamp localization sites are outside the fragment, so both controls visibly do nothing until an unrelated interaction forces a full rerun — the toggle looks broken though it self-heals.
- **Fix:** Call st.rerun(scope='app') from both handlers when the value changed, or move the two display-preference controls out of the fragment (keep the query-heavy saved-views UI inside).

### B36. [LOW · CONFIRMED · APP] drill_report streak counts consecutive passing events, not consecutive months
- **Surface:** Admin → drill scoreboard KPI · `app/logic/drill.py:31`
- The loop walks events newest-first and increments per passing EVENT, so months with no drill event — the exact failure mode the scoreboard exists to catch — do not break the streak and double events double-count: Jan/Feb pass + 4 silent months + Jul pass reports 'Drill streak 3 month(s)' with an ok badge.
- **Fix:** Walk calendar months from RAISED_AT.dt.to_period('M') (a month passes iff it has >=1 event whose latest has NOTIFIED_AT and ACK_AT) and count consecutive passing months back from the most recent, breaking on any missing or failed month.

### B37. [LOW · CONFIRMED · APP] Alert closed-loop savings row omits FINDING_TYPE/TARGET_OBJECT, so the V053 verifier never measures it despite the note promising it
- **Surface:** Alerts drawer → closed-loop SAVINGS_LEDGER booking · `app/ui/pages/alerts.py:311`
- The drawer's ledger insert matches neither of SP_VERIFY_IDLE_SAVINGS's selectors (FINDING_TYPE='AUTO_SUSPEND' or 'Auto-suspend tune: %' description) while the sibling optimize.py booker stamps both with an explicit V053 comment — the row is invisible to the verifier forever, leaving the loop-status chip stuck on ESTIMATED and the 'verifier measures actuals' note false.
- **Fix:** Match optimize.py parity: add FINDING_TYPE ('AUTO_SUSPEND' for the Tighten fix; kind-appropriate values for the others) and TARGET_OBJECT = wh_inline to the drawer insert, and either book a real monthly estimate or reword NOTES to say the V038 change scan settles the measured row.

### B38. [LOW · CONFIRMED · APP] Auto-suspend closed-loop fix shows the CLUSTER_RANGE reverse hint
- **Surface:** Alerts drawer → fix reverse hint · `app/ui/pages/alerts.py:291`
- The two-way kind test ('STATEMENT_TIMEOUT' in stmt else 'CLUSTER_RANGE') never matches the Tighten-auto-suspend statement, so the operator is shown the MIN/MAX_CLUSTER_COUNT undo lever for an AUTO_SUSPEND change even though reverse_hint supports the correct kind and is simply never passed it.
- **Fix:** Make the mapping three-way ('AUTO_SUSPEND' in stmt_cl as the middle arm) or map directly from fix_kind, which already discriminates the built statement.

### B39. [LOW · CONFIRMED · APP] Single-event legacy lifecycle fallback splits the script on ';' inside the note literal — status update without audit row
- **Surface:** Alerts lifecycle (pre-V051 compatibility path) · `app/ui/pages/alerts.py:407`
- The naive split on ';' breaks the audit INSERT mid-literal when the operator note contains a semicolon (sql_literal escapes quotes, not semicolons): the UPDATE commits the status change but the audit row is lost — exactly the partial failure the V051 layer exists to prevent; the bulk path already passes the intact pair.
- **Fix:** Make _lifecycle_sql return the (update, audit) pair like _bulk_lifecycle_sql and pass execute_action(call, [update, audit], ...), deleting the naive split (keep a joined string only for the 'SQL that will run' display).

### B40. [LOW · CONFIRMED · APP] rank_actions marks actions overdue from 00:00 of their due date
- **Surface:** Overview → Top actions ranking · `app/logic/actions.py:43`
- DUE_DATE parses to midnight and 'due < now' flags an action overdue from 00:01 of its due day — a full day before the EOD deadline passes — defeating the adjacent comment's stated intent; effect is confined to sort order among same-severity peers.
- **Fix:** Compare dates, not timestamps: view['_OVERDUE'] = (due.notna() & (due.dt.normalize() < now.normalize())).astype(int).

### B41. [LOW · CONFIRMED · MIG] Scan degradation self-alert says 'of 17 alert rule block(s)' while the proc has 19 blocks
- **Surface:** SP_ALERT_SCAN OPS_SCAN_DEGRADED event title · `snowflake/migrations/V060__family_elapsed_queued_alert_guard.sql:1593`
- The event title hardcodes '17' from the V043-era block count while the same proc has 19 numbered blocks and its RETURN string correctly reports /19 — the self-alert understates the scan surface; pure label defect.
- **Fix:** In the next SP_ALERT_SCAN re-derivation change the literal to 19 — or derive both the title and the RETURN string from one DECLAREd constant so the counts cannot drift again.
